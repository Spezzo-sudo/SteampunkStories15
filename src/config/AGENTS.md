# Config Directory Guidelines
- Configuration modules should only export plain objects and helper getters; avoid instantiating classes or running side effects.
- Document each exported constant or function with JSDoc describing the intent and units.
